import type { NextPage } from "next";
import Head from "next/head";
import Nav from "components/Nav";

const Home: NextPage = () => {
  return (
    <div>

      <Nav />
    </div>
  );
};

export default Home;
